import { TaskListPipePipe } from './task-list-pipe.pipe';

describe('TaskListPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TaskListPipePipe();
    expect(pipe).toBeTruthy();
  });
});
